import React from 'react';

/** Flat Apple grouped-background ground. (File name kept so App.jsx imports it unchanged.) */
export default function DreamBackdrop() {
  return <div aria-hidden="true" className="absolute inset-0 -z-0 bg-[#1C1C1E]" />;
}
